# Core
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# Ollama integration (chat models + embeddings)
langchain-ollama>=0.1.0

# Structured output / validation
pydantic>=2.0

# RAG: vector store + document handling
langchain-chroma>=0.1.0
chromadb>=0.5.0

# Optional: nicer document loaders used in the RAG module
# (the examples fall back to plain-text loading if these are missing)
# pypdf>=4.0
